export interface Country {
  countryId: number;
  country: string;
  telCode: string;
}

export interface State {
  stateId: number;
  state: string;
}

export interface City {
  cityId: number;
  city: string;
}