export interface Category {
    categoryName: string;
    categoryId: number;
    thumbnail: string | null;
    icon: string | null;
}

export interface SubCategory { 
    title: string;
    id: number;
    thumbnail: string | null;
    icon: string | null;
}